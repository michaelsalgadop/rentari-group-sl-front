import PropTypes from "prop-types";
const schemaBtnBack = {
  /**
   * Ruta a la que navegar al hacer clic en el botón de retroceso.
   * Ejemplo: "/search" o "/home"
   */
  urlTo: PropTypes.string.isRequired,
};
export { schemaBtnBack };
